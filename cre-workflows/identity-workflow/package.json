{
    "name": "mirage-identity-workflow",
    "version": "0.1.0",
    "private": true,
    "description": "CRE Identity Workflow — World ID HTTP Verify → DON Consensus → IdentityGate Onchain Write",
    "main": "main.ts",
    "scripts": {
        "postinstall": "bunx cre-setup",
        "simulate": "cre workflow simulate .",
        "build": "cre workflow build ."
    },
    "dependencies": {
        "@chainlink/cre-sdk": "latest"
    },
    "engines": {
        "bun": ">=1.3.0"
    }
}